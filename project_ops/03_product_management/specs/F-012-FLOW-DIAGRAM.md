# F-012: Duplicate Content Viewer - User Flow Diagram

## Flow Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                                │
└─────────────────────────────────────────────────────────────────────┘

1. SEARCH/BROWSE
   │
   ├─→ User searches for "AI conferences"
   │   or browses Events feed
   │
   └─→ Backend uses Qdrant Grouping API
       to deduplicate results
       │
       ├─→ Groups by canonical_item_id
       ├─→ Returns 1 canonical item per group
       └─→ Includes all sources in response

2. VIEW RESULTS
   │
   ├─→ User sees content cards with badges
   │   │
   │   ├─→ Single source: Simple email link
   │   │   "📧 AI Weekly"
   │   │
   │   ├─→ 2-3 sources: Blue badge (moderate)
   │   │   "📊 Seen in 2 sources"
   │   │
   │   ├─→ 4-5 sources: Purple badge (popular)
   │   │   "📊 Seen in 4 sources"
   │   │
   │   └─→ 6+ sources: Gradient badge (viral)
   │       "🔥 Seen in 8 sources" (pulsing)
   │
   └─→ User notices popular content by badge

3. EXPAND SOURCES
   │
   ├─→ User clicks badge to expand
   │   │
   │   └─→ Source list slides down (200ms animation)
   │       │
   │       ├─→ Shows all sources sorted by date
   │       ├─→ Each source shows:
   │       │   - Email icon
   │       │   - Sender name
   │       │   - Relative date
   │       │   - "View Email" button
   │       │
   │       └─→ User can see content popularity
   │           and source diversity
   │
   └─→ User decides which source to view

4. VIEW SOURCE EMAIL
   │
   ├─→ User clicks "View Email" on any source
   │   │
   │   └─→ EmailModal opens (from F-008)
   │       │
   │       ├─→ Shows source navigation header
   │       │   "Source 2 of 5: ML News"
   │       │   [◀ Prev] [Next ▶]
   │       │
   │       ├─→ Displays email metadata
   │       │   From, To, Subject, Date
   │       │
   │       ├─→ Renders email body (HTML)
   │       │   - Sanitized for security
   │       │   - Images loaded
   │       │   - Links clickable
   │       │
   │       └─→ Shows action buttons
   │           [Open in Gmail] [Copy Link]
   │
   └─→ User reads original email

5. NAVIGATE SOURCES
   │
   ├─→ User clicks "Next ▶" or presses →
   │   │
   │   └─→ Modal updates to next source
   │       - Email content changes
   │       - Counter updates "Source 3 of 5"
   │       - Prev button enables
   │
   ├─→ User compares multiple sources
   │   - Verifies information
   │   - Sees different perspectives
   │   - Finds most detailed version
   │
   └─→ User closes modal (Esc or ✕)
       Returns to content feed
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                      COMPONENT ARCHITECTURE                         │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          ContentCard                                │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Title: "NeurIPS 2025 Conference"                             │  │
│  │  Date: Dec 10-16, 2025                                        │  │
│  │  Location: Vancouver, Canada                                  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │            DuplicateSourceBadge                               │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  🔥 Seen in 8 sources                          [Expand]  │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  │                                                               │  │
│  │  {isExpanded && (                                            │  │
│  │    ┌───────────────────────────────────────────────────────┐ │  │
│  │    │  SourceList                                           │ │  │
│  │    │  ├─ SourceListItem (AI Weekly)    [View Email]       │ │  │
│  │    │  ├─ SourceListItem (ML News)      [View Email]       │ │  │
│  │    │  ├─ SourceListItem (Tech Digest)  [View Email]       │ │  │
│  │    │  └─ ...                                               │ │  │
│  │    └───────────────────────────────────────────────────────┘ │  │
│  │  )}                                                           │  │
│  │                                                               │  │
│  │  {selectedEmailId && (                                       │  │
│  │    <EmailModal                                               │  │
│  │      emailId={selectedEmailId}                               │  │
│  │      sources={sources}                                       │  │
│  │      currentSourceIndex={index}                              │  │
│  │      onNavigateSource={handleNavigate}                       │  │
│  │    />                                                        │  │
│  │  )}                                                           │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Tags: #AI #Conference #MachineLearning                       │  │
│  │  [View Details]                                               │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘

                              │
                              │ User clicks "View Email"
                              ▼

┌─────────────────────────────────────────────────────────────────────┐
│                          EmailModal                                 │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  ✕                                                            │  │
│  │  Source 2 of 5: ML News              [◀ Prev]    [Next ▶]    │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  From: ML News <newsletter@mlnews.com>                        │  │
│  │  To: user@example.com                                         │  │
│  │  Subject: Top ML Conferences 2025                             │  │
│  │  Date: Nov 27, 2024 at 2:15 PM                                │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                                                               │  │
│  │  [Email Body - HTML Rendered]                                │  │
│  │                                                               │  │
│  │  Lorem ipsum dolor sit amet, consectetur adipiscing elit...  │  │
│  │                                                               │  │
│  │  [Scrollable content...]                                     │  │
│  │                                                               │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  [Open in Gmail]  [Copy Link]                                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                        STATE FLOW                                   │
└─────────────────────────────────────────────────────────────────────┘

DuplicateSourceBadge State:
┌─────────────────────────────────────┐
│  isExpanded: boolean                │
│  selectedEmailId: string | null     │
└─────────────────────────────────────┘
         │
         ├─→ User clicks badge
         │   └─→ setIsExpanded(!isExpanded)
         │
         ├─→ User clicks "View Email"
         │   └─→ setSelectedEmailId(source.email_id)
         │       └─→ Opens EmailModal
         │
         └─→ User closes modal
             └─→ setSelectedEmailId(null)

EmailModal State:
┌─────────────────────────────────────┐
│  emailData: EmailData | null        │
│  isLoading: boolean                 │
│  error: Error | null                │
└─────────────────────────────────────┘
         │
         ├─→ Modal opens
         │   └─→ Fetch email by emailId
         │       ├─→ setIsLoading(true)
         │       ├─→ API call: GET /api/emails/{emailId}
         │       └─→ setEmailData(response)
         │           setIsLoading(false)
         │
         ├─→ User navigates to next source
         │   └─→ onNavigateSource(currentIndex + 1)
         │       └─→ Parent updates selectedEmailId
         │           └─→ EmailModal re-fetches new email
         │
         └─→ User closes modal
             └─→ onClose()
                 └─→ Parent sets selectedEmailId = null
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW                                   │
└─────────────────────────────────────────────────────────────────────┘

1. USER SEARCH
   │
   ▼
┌─────────────────────────────────────┐
│  Frontend: SearchBar                │
│  query = "AI conferences"           │
└─────────────────────────────────────┘
   │
   │ POST /api/search
   │ { query: "AI conferences", type: "events" }
   │
   ▼
┌─────────────────────────────────────┐
│  Backend: SearchService             │
│  - Generate query vector            │
│  - Call Qdrant Grouping API         │
│  - Group by canonical_item_id       │
└─────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────┐
│  Qdrant: Grouping API               │
│  - Search with vector               │
│  - Group results by canonical_id    │
│  - Return 1 item per group          │
│  - Include all group members        │
└─────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────┐
│  Backend: _get_sources_for_item     │
│  - For each canonical_item_id       │
│  - Scroll collection with filter    │
│  - Extract source metadata          │
│  - Sort by received_at              │
└─────────────────────────────────────┘
   │
   │ Response:
   │ {
   │   results: [
   │     {
   │       id: "item-123",
   │       title: "NeurIPS 2025",
   │       canonical_item_id: "abc123",
   │       is_duplicate: true,
   │       source_count: 8,
   │       sources: [
   │         { email_id: "e1", sender: "AI Weekly", ... },
   │         { email_id: "e2", sender: "ML News", ... },
   │         ...
   │       ]
   │     }
   │   ]
   │ }
   │
   ▼
┌─────────────────────────────────────┐
│  Frontend: ContentCard              │
│  - Render DuplicateSourceBadge      │
│  - Pass sources array               │
└─────────────────────────────────────┘
   │
   │ User clicks "View Email"
   │
   ▼
┌─────────────────────────────────────┐
│  Frontend: EmailModal               │
│  GET /api/emails/{email_id}         │
└─────────────────────────────────────┘
   │
   ▼
┌─────────────────────────────────────┐
│  Backend: GmailService              │
│  - Fetch email from Gmail API       │
│  - Parse headers, body, attachments │
│  - Sanitize HTML                    │
│  - Return structured data           │
└─────────────────────────────────────┘
   │
   │ Response:
   │ {
   │   id: "e1",
   │   from: "AI Weekly <...>",
   │   subject: "...",
   │   body_html: "<html>...",
   │   ...
   │ }
   │
   ▼
┌─────────────────────────────────────┐
│  Frontend: EmailModal               │
│  - Render email content             │
│  - Enable source navigation         │
└─────────────────────────────────────┘
```

---

## Decision Tree

```
┌─────────────────────────────────────────────────────────────────────┐
│                      RENDERING LOGIC                                │
└─────────────────────────────────────────────────────────────────────┘

When rendering a FeedItem:

sources.length === 0
   │
   └─→ Show "No source available" (error state)

sources.length === 1
   │
   └─→ Render simple email link
       ┌─────────────────────────────────┐
       │  📧 AI Weekly                   │
       │  [View Source Email]            │
       └─────────────────────────────────┘

sources.length === 2 or 3
   │
   └─→ Render MODERATE badge (blue)
       ┌─────────────────────────────────┐
       │  📊 Seen in 2 sources  [Expand] │
       └─────────────────────────────────┘

sources.length === 4 or 5
   │
   └─→ Render POPULAR badge (purple)
       ┌─────────────────────────────────┐
       │  📊 Seen in 4 sources  [Expand] │
       └─────────────────────────────────┘

sources.length >= 6
   │
   └─→ Render VIRAL badge (gradient + pulse)
       ┌─────────────────────────────────┐
       │  🔥 Seen in 8 sources  [Expand] │
       └─────────────────────────────────┘

When badge is clicked:

isExpanded === false
   │
   └─→ setIsExpanded(true)
       └─→ Animate source list slide down
           └─→ Render all SourceListItems

isExpanded === true
   │
   └─→ setIsExpanded(false)
       └─→ Animate source list slide up
           └─→ Hide source list

When "View Email" is clicked:

   │
   └─→ setSelectedEmailId(source.email_id)
       └─→ Render EmailModal
           ├─→ Fetch email data
           ├─→ Show loading skeleton
           └─→ Render email content

When navigating sources in modal:

User clicks "Next"
   │
   └─→ currentIndex < sources.length - 1
       ├─→ YES: onNavigateSource(currentIndex + 1)
       │        └─→ Update selectedEmailId
       │            └─→ Fetch new email
       │
       └─→ NO: Disable "Next" button

User clicks "Prev"
   │
   └─→ currentIndex > 0
       ├─→ YES: onNavigateSource(currentIndex - 1)
       │        └─→ Update selectedEmailId
       │            └─→ Fetch new email
       │
       └─→ NO: Disable "Prev" button
```

---

## Performance Optimization Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PERFORMANCE STRATEGY                             │
└─────────────────────────────────────────────────────────────────────┘

Backend:
   │
   ├─→ Use Qdrant Grouping API (native deduplication)
   │   └─→ Faster than post-search deduplication
   │       └─→ Target: < 500ms for search with grouping
   │
   ├─→ Cache email data (Redis, 1 hour TTL)
   │   └─→ Avoid repeated Gmail API calls
   │       └─→ Target: < 100ms for cached email
   │
   └─→ Limit sources per item (max 10)
       └─→ Prevent payload bloat
           └─→ Add "Load more" if needed

Frontend:
   │
   ├─→ Lazy load EmailModal
   │   └─→ Only fetch email when modal opens
   │       └─→ Don't prefetch all sources upfront
   │
   ├─→ Prefetch adjacent sources
   │   └─→ When modal opens, prefetch next/prev email
   │       └─→ Makes navigation feel instant
   │
   ├─→ Virtualize long source lists
   │   └─→ If > 20 sources, use virtual scrolling
   │       └─→ Only render visible items
   │
   └─→ Debounce expand/collapse
       └─→ Prevent rapid toggling
           └─→ 200ms debounce on badge click
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                      ERROR SCENARIOS                                │
└─────────────────────────────────────────────────────────────────────┘

Scenario 1: Email Deleted from Gmail
   │
   ├─→ User clicks "View Email"
   │   └─→ GET /api/emails/{email_id}
   │       └─→ Gmail API returns 404
   │           └─→ Backend returns error
   │               └─→ Frontend shows error state
   │                   ┌─────────────────────────────────┐
   │                   │  ⚠️ Email no longer available  │
   │                   │  This email may have been      │
   │                   │  deleted from Gmail.           │
   │                   │  [Close]                       │
   │                   └─────────────────────────────────┘

Scenario 2: Network Error
   │
   ├─→ User clicks "View Email"
   │   └─→ GET /api/emails/{email_id}
   │       └─→ Network timeout
   │           └─→ Frontend shows error with retry
   │               ┌─────────────────────────────────┐
   │               │  ⚠️ Failed to load email       │
   │               │  [Retry]  [Close]              │
   │               └─────────────────────────────────┘

Scenario 3: No Sources Available
   │
   ├─→ Backend returns item with sources: []
   │   └─→ Frontend shows warning
   │       ┌─────────────────────────────────┐
   │       │  ⚠️ No source information       │
   │       │  available for this item        │
   │       └─────────────────────────────────┘

Scenario 4: Partial Source Data
   │
   ├─→ Some sources missing sender or date
   │   └─→ Frontend shows "Unknown" for missing fields
   │       └─→ Still allows viewing email if email_id exists
```

This comprehensive flow diagram should help you visualize exactly how the feature works from end to end!
